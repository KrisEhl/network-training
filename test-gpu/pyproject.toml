[project]
name = "test-gpu"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "network-training",
    "torch>=2.10.0",
]

[tool.uv.sources]
network-training = { workspace = true }
